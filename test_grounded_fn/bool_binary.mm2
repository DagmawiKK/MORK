(match idk)

(exec 0
    (, (match $x))
    (O
        (pure
            (false Implies false => $r)
            $r
            (implies_bool (bool_from_string false) (bool_from_string false))
        )
    )
)

(exec 1
    (, (match $x))
    (O
        (pure
            (true Implies false => $r)
            $r
            (implies_bool (bool_from_string true) (bool_from_string false))
        )
    )
)
(exec 2
    (, (match $x))
    (O
        (pure
            (false Implies true => $r)
            $r
            (implies_bool (bool_from_string false) (bool_from_string true))
        )
    )
)

(exec 3
    (, (match $x))
    (O
        (pure
            (true Implies true => $r)
            $r
            (implies_bool (bool_from_string true) (bool_from_string true))
        )
    )
)